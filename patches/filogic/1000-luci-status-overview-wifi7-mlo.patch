diff --git a/feeds/luci/modules/luci-base/root/usr/share/rpcd/ucode/luci b/feeds/luci/modules/luci-base/root/usr/share/rpcd/ucode/luci
--- a/feeds/luci/modules/luci-base/root/usr/share/rpcd/ucode/luci
+++ b/feeds/luci/modules/luci-base/root/usr/share/rpcd/ucode/luci
@@ -46,6 +46,125 @@ function callPackageVersionCheck(pkg) {
 	return version;
 }
 
+function normalizeMac(mac) {
+	return mac ? uc(mac) : '';
+}
+
+function getHostHintByMac(hints, mac) {
+	let key = normalizeMac(mac);
+
+	if (!length(key))
+		return null;
+
+	return hints?.[key] ?? hints?.[lc(key)] ?? null;
+}
+
+function getWifiStationMloAliases() {
+	let aliases = {};
+	let fd = popen('logread 2>/dev/null');
+
+	if (!fd)
+		return aliases;
+
+	for (let line = fd.read('line'); length(line); line = fd.read('line')) {
+		let link = null;
+		let mld = null;
+		let m = match(line, /mld\(sta_idx=[0-9]+,addr=([0-9A-Fa-f:]{17})\).*link\(([0-9A-Fa-f:]{17})\)/);
+
+		if (m) {
+			mld = normalizeMac(m[1]);
+			link = normalizeMac(m[2]);
+		}
+		else if ((m = match(line, /mac=([0-9A-Fa-f:]{17}), mld\(sta_idx=[0-9]+,addr=([0-9A-Fa-f:]{17})\)/)) != null) {
+			link = normalizeMac(m[1]);
+			mld = normalizeMac(m[2]);
+		}
+
+		if (length(link) && length(mld) && link != mld)
+			aliases[link] = mld;
+	}
+
+	fd.close();
+
+	return aliases;
+}
+
+function pushUniqueAddress(addrs, addr) {
+	if (!addr)
+		return addrs;
+
+	addrs ??= [];
+
+	for (let existing in addrs)
+		if (existing == addr)
+			return addrs;
+
+	push(addrs, addr);
+
+	return addrs;
+}
+
+function getWifiStationLeaseHints() {
+	let hints = {};
+	let fd = open('/tmp/dhcp.leases', 'r');
+
+	if (!fd)
+		return hints;
+
+	for (let line = fd.read('line'); length(line); line = fd.read('line')) {
+		let fields = split(trim(line), ' ');
+		let mac = normalizeMac(fields[1]);
+		let ip = fields[2];
+		let name = fields[3];
+
+		if (!length(mac))
+			continue;
+
+		hints[mac] ??= {};
+
+		if (name && name != '*')
+			hints[mac].name ??= name;
+
+		if (ip && ip != '*')
+			hints[mac].ipaddrs = pushUniqueAddress(hints[mac].ipaddrs, ip);
+	}
+
+	fd.close();
+
+	return hints;
+}
+
+function appendWifiStationNeighborHints(hints) {
+	let fd = popen('ip neigh show 2>/dev/null; ip -6 neigh show 2>/dev/null');
+
+	if (!fd)
+		return hints;
+
+	for (let line = fd.read('line'); length(line); line = fd.read('line')) {
+		let m = match(trim(line), /^(\S+)\s+dev\s+\S+\s+lladdr\s+([0-9A-Fa-f:]{17})\s+/);
+
+		if (!m)
+			continue;
+
+		let addr = m[1];
+		let mac = normalizeMac(m[2]);
+
+		if (!length(mac))
+			continue;
+
+		hints[mac] ??= {};
+
+		if (match(addr, /:/))
+			hints[mac].ip6addrs = pushUniqueAddress(hints[mac].ip6addrs, addr);
+		else
+			hints[mac].ipaddrs = pushUniqueAddress(hints[mac].ipaddrs, addr);
+	}
+
+	fd.close();
+
+	return hints;
+}
+
 const methods = {
 	getVersion: {
 		call: function(request) {
@@ -695,6 +814,34 @@ const methods = {
 		}
 	},
 
+	getWifiStationHints: {
+		call: function() {
+			const hosthints = appendWifiStationNeighborHints(getWifiStationLeaseHints());
+			const aliases = getWifiStationMloAliases();
+			let result = {};
+
+			for (let link, source in aliases) {
+				let hint = hosthints?.[source] ?? hosthints?.[lc(source)] ?? null;
+
+				if (!hint)
+					continue;
+
+				result[link] = { source_mac: source };
+
+				if (hint.name)
+					result[link].name = hint.name;
+
+				if (hint.ipaddrs || hint.ipv4)
+					result[link].ipaddrs = hint.ipaddrs || hint.ipv4;
+
+				if (hint.ip6addrs || hint.ipv6)
+					result[link].ip6addrs = hint.ip6addrs || hint.ipv6;
+			}
+
+			return result;
+		}
+	},
+
 	getOnlineUsers: {
 		call: function() {
 			const fd = open('/proc/net/arp', 'r');
diff --git a/feeds/luci/modules/luci-mod-status/htdocs/luci-static/resources/view/status/include/60_wifi.js b/feeds/luci/modules/luci-mod-status/htdocs/luci-static/resources/view/status/include/60_wifi.js
--- a/feeds/luci/modules/luci-mod-status/htdocs/luci-static/resources/view/status/include/60_wifi.js
+++ b/feeds/luci/modules/luci-mod-status/htdocs/luci-static/resources/view/status/include/60_wifi.js
@@ -25,31 +25,80 @@ return baseclass.extend({
 		expect: { 'access': false }
 	}),
 
+	callWifiStationHints: rpc.declare({
+		object: 'luci',
+		method: 'getWifiStationHints',
+		expect: { '': {} }
+	}),
+
 	wifirate: function(rt) {
-		var s = '%.1f\xa0%s, %d\xa0%s'.format(rt.rate / 1000, _('Mbit/s'), rt.mhz, _('MHz')),
+		if (!rt || rt.rate == null || rt.rate <= 0)
+			return '-';
+
+		var s = '%.1f\xa0%s'.format(rt.rate / 1000, _('Mbit/s')),
 		    ht = rt.ht, vht = rt.vht,
-			mhz = rt.mhz, nss = rt.nss,
+			nss = rt.nss,
 			mcs = rt.mcs, sgi = rt.short_gi,
 			he = rt.he, he_gi = rt.he_gi,
-			he_dcm = rt.he_dcm;
+			he_dcm = rt.he_dcm,
+			eht = (rt.eht === true),
+			eht_gi = rt.eht_gi,
+			eht_dcm = rt.eht_dcm;
+
+		if (rt.mhz != null)
+			s += ', %d\xa0%s'.format(rt.mhz, _('MHz'));
 
 		if (ht || vht) {
-			if (vht) s += ', VHT-MCS\xa0%d'.format(mcs);
-			if (nss) s += ', VHT-NSS\xa0%d'.format(nss);
-			if (ht)  s += ', MCS\xa0%s'.format(mcs);
+			if (vht && mcs != null) s += ', VHT-MCS\xa0%d'.format(mcs);
+			if (vht && nss != null) s += ', VHT-NSS\xa0%d'.format(nss);
+			if (ht && mcs != null)  s += ', MCS\xa0%s'.format(mcs);
 			if (sgi) s += ', ' + _('Short GI').replace(/ /g, '\xa0');
 		}
 
 		if (he) {
-			s += ', HE-MCS\xa0%d'.format(mcs);
-			if (nss) s += ', HE-NSS\xa0%d'.format(nss);
-			if (he_gi) s += ', HE-GI\xa0%d'.format(he_gi);
-			if (he_dcm) s += ', HE-DCM\xa0%d'.format(he_dcm);
+			if (mcs != null) s += ', HE-MCS\xa0%d'.format(mcs);
+			if (nss != null) s += ', HE-NSS\xa0%d'.format(nss);
+			if (he_gi != null) s += ', HE-GI\xa0%d'.format(he_gi);
+			if (he_dcm != null) s += ', HE-DCM\xa0%d'.format(he_dcm);
+		}
+
+		if (eht) {
+			if (mcs != null) s += ', EHT-MCS\xa0%d'.format(mcs);
+			if (nss != null) s += ', EHT-NSS\xa0%d'.format(nss);
+			if (eht_gi != null) s += ', EHT-GI\xa0%d'.format(eht_gi);
+			if (eht_dcm != null) s += ', EHT-DCM\xa0%d'.format(eht_dcm);
 		}
 
 		return s;
 	},
 
+	getStationHint: function(hosthints, stationhints, mac) {
+		var alias = stationhints[(mac || '').toUpperCase()] || stationhints[mac] || null,
+		    source_mac = alias ? alias.source_mac : null,
+		    name = hosthints.getHostnameByMACAddr(mac),
+		    ipv4 = hosthints.getIPAddrByMACAddr(mac),
+		    ipv6 = hosthints.getIP6AddrByMACAddr(mac);
+
+		if (!name && alias)
+			name = alias.name || (source_mac ? hosthints.getHostnameByMACAddr(source_mac) : null);
+
+		if (!ipv4 && alias)
+			ipv4 = L.toArray(alias.ipaddrs || alias.ipv4)[0] || (source_mac ? hosthints.getIPAddrByMACAddr(source_mac) : null);
+
+		if (!ipv6 && alias)
+			ipv6 = L.toArray(alias.ip6addrs || alias.ipv6)[0] || (source_mac ? hosthints.getIP6AddrByMACAddr(source_mac) : null);
+
+		if (name == '?')
+			name = null;
+
+		return {
+			name: name,
+			ipv4: ipv4,
+			ipv6: ipv6,
+			source_mac: source_mac
+		};
+	},
+
 	handleDelClient: function(wifinet, mac, ev, cmd) {
 		var exec = cmd || 'disconnect';
 
@@ -183,6 +229,7 @@ return baseclass.extend({
 			network.getWifiDevices(),
 			network.getWifiNetworks(),
 			network.getHostHints(),
+			L.resolveDefault(this.callWifiStationHints(), {}),
 			this.callSessionAccess('access-group', 'luci-mod-status-index-wifi', 'read'),
 			this.callSessionAccess('access-group', 'luci-mod-status-index-wifi', 'write'),
 			firewall.getZones(),
@@ -217,9 +264,10 @@ return baseclass.extend({
 		    radios = data[0],
 		    networks = data[1],
 		    hosthints = data[2],
-		    hasReadPermission = data[3],
-		    hasWritePermission = data[4],
-		    zones = data[5];
+		    stationhints = data[3],
+		    hasReadPermission = data[4],
+		    hasWritePermission = data[5],
+		    zones = data[6];
 
 		var table = E('div', { 'class': 'network-status-table' });
 
@@ -256,9 +304,10 @@ return baseclass.extend({
 
 			for (var k = 0; k < networks[i].assoclist.length; k++) {
 				var bss = networks[i].assoclist[k],
-				    name = hosthints.getHostnameByMACAddr(bss.mac),
-				    ipv4 = hosthints.getIPAddrByMACAddr(bss.mac),
-				    ipv6 = hosthints.getIP6AddrByMACAddr(bss.mac);
+				    host = this.getStationHint(hosthints, stationhints, bss.mac),
+				    name = host.name,
+				    ipv4 = host.ipv4,
+				    ipv6 = host.ipv6;
 
 				var icon;
 				var q = Math.min((bss.signal + 110) / 70 * 100, 100);
diff --git a/feeds/luci/modules/luci-mod-status/root/usr/share/rpcd/acl.d/luci-mod-status-index.json b/feeds/luci/modules/luci-mod-status/root/usr/share/rpcd/acl.d/luci-mod-status-index.json
--- a/feeds/luci/modules/luci-mod-status/root/usr/share/rpcd/acl.d/luci-mod-status-index.json
+++ b/feeds/luci/modules/luci-mod-status/root/usr/share/rpcd/acl.d/luci-mod-status-index.json
@@ -60,7 +60,8 @@
 		"description": "Grant access to wireless status display",
 		"read": {
 			"ubus": {
-				"iwinfo": [ "assoclist" ]
+				"iwinfo": [ "assoclist" ],
+				"luci": [ "getWifiStationHints" ]
 			}
 		},
 		"write": {
